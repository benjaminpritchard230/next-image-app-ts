import PrivateUserCard from "@/components/PrivateUserCard";
import React from "react";

type Props = {};

const MyProfile = (props: Props) => {
  return <PrivateUserCard />;
};

export default MyProfile;
